pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'sleep 5'
            }
        }

        stage('Test') {
            steps {
                sh '''
                    #!/bin/bash
                    ls -lrt
                    sleep 5
                 '''   

            }

        }
        stage('Trigger Maven-pipeline Job') {
            steps {
                 echo "chmod +x agent-1/Mavenfile"

                sh './agent-1/Mavenfile clean install'
                build job: 'Maven-pipeline', wait: true
                echo 'Maven-pipeline job triggered successfully!'
            }
        }
    }   
}
    